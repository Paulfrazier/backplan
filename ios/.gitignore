# XcodeGen output (regenerate with `xcodegen generate`)
*.xcodeproj

# Build output
build/
build-*/
DerivedData/

# macOS
.DS_Store

# Xcode user state
xcuserdata/
*.xcworkspace/xcuserdata/

# TestFlight credentials — App Store Connect key id / issuer id.
# The .p8 itself lives in ~/.appstoreconnect/private_keys/, never in the repo.
.testflight.env
